# Git 屬性設定檔
# 此檔案用於定義專案中特定路徑的屬性，以確保跨平台開發的一致性。

# 自動偵測文字檔案，並確保它們在工作目錄中使用作業系統的原生換行符，
# 但在 Git 倉庫中統一儲存為 LF。
* text=auto

# --- 強制特定檔案類型的換行符 ---
# 對於 Shell 腳本，無論在哪個平台編輯，都強制使用 LF (Unix) 換行符。
# 這對於避免在 Linux/macOS 的 Docker 容器中執行時，因 CRLF 換行符導致的錯誤至關重要。
*.sh eol=lf